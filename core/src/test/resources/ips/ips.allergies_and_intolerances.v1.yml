grammar: FHIRConnect/v0.9.0
type: extension
metadata:
  name: IPS_adverse_reaction
  version: 0.0.1-alpha
spec:
  system: FHIR
  version: R4
  extends: EVALUATION.adverse_reaction_risk.v2

mappings:
  - name: "dateTime"
    extension: add
    with:
      fhir: "$resource.abatement.as(DateTime)"
      openehr: "$archetype/data[at0001]/items[at0030]"